ADD file:4dd75f16753c9c921fd05b1d0ed5b425d74d87405a76a0b3afcbf9723a50d1ce in / 
/bin/sh -c set -xe 		&& echo '#!/bin/sh' > /usr/sbin/policy-rc.d 	&& echo 'exit 101' >> /usr/sbin/policy-rc.d 	&& chmod +x /usr/sbin/policy-rc.d 		&& dpkg-divert --local --rename --add /sbin/initctl 	&& cp -a /usr/sbin/policy-rc.d /sbin/initctl 	&& sed -i 's/^exit.*/exit 0/' /sbin/initctl 		&& echo 'force-unsafe-io' > /etc/dpkg/dpkg.cfg.d/docker-apt-speedup 		&& echo 'DPkg::Post-Invoke { "rm -f /var/cache/apt/archives/*.deb /var/cache/apt/archives/partial/*.deb /var/cache/apt/*.bin || true"; };' > /etc/apt/apt.conf.d/docker-clean 	&& echo 'APT::Update::Post-Invoke { "rm -f /var/cache/apt/archives/*.deb /var/cache/apt/archives/partial/*.deb /var/cache/apt/*.bin || true"; };' >> /etc/apt/apt.conf.d/docker-clean 	&& echo 'Dir::Cache::pkgcache ""; Dir::Cache::srcpkgcache "";' >> /etc/apt/apt.conf.d/docker-clean 		&& echo 'Acquire::Languages "none";' > /etc/apt/apt.conf.d/docker-no-languages 		&& echo 'Acquire::GzipIndexes "true"; Acquire::CompressionTypes::Order:: "gz";' > /etc/apt/apt.conf.d/docker-gzip-indexes 		&& echo 'Apt::AutoRemove::SuggestsImportant "false";' > /etc/apt/apt.conf.d/docker-autoremove-suggests
/bin/sh -c rm -rf /var/lib/apt/lists/*
/bin/sh -c mkdir -p /run/systemd && echo 'docker' > /run/systemd/container
 CMD ["/bin/bash"]
/bin/sh -c apt-get update && apt-get install -q -y --no-install-recommends     dirmngr     gnupg2     && rm -rf /var/lib/apt/lists/*
/bin/sh -c echo "deb http://snapshots.ros.org/kinetic/final/ubuntu xenial main" > /etc/apt/sources.list.d/ros1-snapshots.list
/bin/sh -c apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 4B63CF8FDE49746E98FA01DDAD19BAB3CBF125EA
 ENV LANG=C.UTF-8
 ENV LC_ALL=C.UTF-8
 ENV ROS_DISTRO=kinetic
/bin/sh -c apt-get update && apt-get install -y --no-install-recommends     ros-kinetic-ros-core=1.3.2-0*     && rm -rf /var/lib/apt/lists/*
COPY file:cbbaa0f5d6a276512315f5b4d7347e94a120cefbda9058ebb0d678847ff4837f in / 
 ENTRYPOINT ["/ros_entrypoint.sh"]
 CMD ["bash"]
/bin/sh -c apt-get update && apt-get install --no-install-recommends -y     build-essential     python-rosdep     python-rosinstall     python-vcstools     && rm -rf /var/lib/apt/lists/*
/bin/sh -c rosdep init &&   rosdep update --rosdistro $ROS_DISTRO
/bin/sh -c apt-get update && apt-get install -y --no-install-recommends     ros-kinetic-ros-base=1.3.2-0*     && rm -rf /var/lib/apt/lists/*
LABEL maintainer=Gonzalo Casas <casas@arch.ethz.ch>
SHELL [/bin/bash -c]
RUN /bin/bash -c apt-get update     && apt-get install -y     git     net-tools     iputils-ping     dnsutils     ros-${ROS_DISTRO}-rosbridge-server     --no-install-recommends     && rm -rf /var/lib/apt/lists/* # buildkit
ENV CATKIN_WS=/root/catkin_ws
RUN /bin/bash -c mkdir -p $CATKIN_WS/src # buildkit
WORKDIR /root/catkin_ws/src
RUN /bin/bash -c source /opt/ros/${ROS_DISTRO}/setup.bash     && apt-get update     && git clone https://github.com/gramaziokohler/ros_file_server.git     && cd $CATKIN_WS     && rosdep install -y --from-paths . --ignore-src --rosdistro ${ROS_DISTRO}     && catkin_make # buildkit
RUN /bin/bash -c echo "source /usr/local/bin/ros_catkin_entrypoint.sh" >> /root/.bashrc # buildkit
COPY ros_catkin_entrypoint.sh /usr/local/bin/ros_catkin_entrypoint.sh # buildkit
RUN /bin/bash -c chmod +x /usr/local/bin/ros_catkin_entrypoint.sh # buildkit
ENTRYPOINT ["/usr/local/bin/ros_catkin_entrypoint.sh"]
CMD ["bash"]
LABEL maintainer=Gonzalo Casas <casas@arch.ethz.ch>
SHELL [/bin/bash -c]
ENV CATKIN_WS=/root/catkin_ws
RUN /bin/bash -c echo "Install MoveIt"     && source /opt/ros/${ROS_DISTRO}/setup.bash     && apt-get update && apt-get -y upgrade     && apt-get install -y ros-${ROS_DISTRO}-moveit --no-install-recommends     && rm -rf /var/lib/apt/lists/* # buildkit
ENV DEBIAN_FRONTEND=noninteractive
RUN /bin/bash -c echo "Building common MoveIt Planners"     && source /opt/ros/${ROS_DISTRO}/setup.bash     && apt-get update && apt-get -y upgrade     && git clone -b kinetic-devel https://github.com/ros-industrial/universal_robot.git     && git clone https://github.com/ros-industrial/abb.git     && git clone https://github.com/ros-industrial/abb_experimental.git     && git clone https://github.com/gramaziokohler/abb_irb4600_40_255.git     && git clone https://github.com/gramaziokohler/abb_irb4600_40_255_moveit_config.git     && git clone https://github.com/gramaziokohler/abb_irb4600_60_205.git     && git clone https://github.com/gramaziokohler/abb_irb4600_60_205_moveit_config.git     && git clone https://github.com/gramaziokohler/rfl_description.git     && git clone -b kinetic-devel https://github.com/ros-planning/moveit_tutorials.git     && git clone -b kinetic-devel https://github.com/ros-planning/panda_moveit_config.git     && cd $CATKIN_WS     && rosdep install -y --from-paths . --ignore-src --skip-keys="abb_driver gazebo" --rosdistro ${ROS_DISTRO}     && catkin_make     && rm -rf /var/lib/apt/lists/* # buildkit
ADD launch /root/catkin_ws/launch # buildkit
LABEL maintainer=Gido Dielemans <gido.dielemans@tum.de>
SHELL [/bin/bash -c]
ENV CATKIN_WS=/root/catkin_ws
RUN /bin/bash -c mkdir -p $CATKIN_WS/src # buildkit
COPY . /root/catkin_ws/src # buildkit
RUN /bin/bash -c echo "Building tools for ROS"     && source /opt/ros/${ROS_DISTRO}/setup.bash     && apt-get update     && git clone https://github.com/augmentedfabricationlab/robotic_setups_description.git     && cd $CATKIN_WS     && rosdep install -y --from-paths . --ignore-src --rosdistro ${ROS_DISTRO}     && catkin_make     && rm -rf /var/lib/apt/lists/* # buildkit